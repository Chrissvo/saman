<div class="breadcrumbs-bar">
  <ul class="breadcrumbs">
    <li>{{#link-to 'index'}}Home{{/link-to}}</li>
    {{#if (not-eq currentPath "index")}}
      {{#each breadCrumbs as |breadCrumb|}}
        <li class="chevron">></li>
        <li>{{breadCrumb}}</li>
      {{/each}}
    {{/if}}
  </ul>
</div>
